# Domain name
DOMAIN=radio.2-way.ru

# Icecast passwords (generate strong passwords!)
# Use: openssl rand -base64 16
ICECAST_SOURCE_PASSWORD=
ICECAST_ADMIN_PASSWORD=
ICECAST_RELAY_PASSWORD=

# adding at server
# echo "DOMAIN=radio.2-way.ru" > .env
# echo "ICECAST_SOURCE_PASSWORD=$(openssl rand -base64 16)" >> .env
# echo "ICECAST_ADMIN_PASSWORD=$(openssl rand -base64 16)" >> .env
# echo "ICECAST_RELAY_PASSWORD=$(openssl rand -base64 16)" >> .env

# create directories
# mkdir -p shared data music
# chmod 777 shared
# echo '{"title": "Starting...", "artist": "", "filename": "", "started_at": 0}' > shared/current_track.json
# chmod 666 shared/current_track.json

# download music on server
# scp -P 22222 music/*.mp3 ${USER}@radio.2-way.ru:~/radio_andrews/music/


# ============================================
# DOCKER
# ============================================
# Run docker on server

# cd ~/radio_andrews

# # Запускаем с production конфигом
# docker compose -f docker-compose.prod.yml up -d

# # Проверяем
# docker compose -f docker-compose.prod.yml ps

# # Смотрим логи
# docker compose -f docker-compose.prod.yml logs -f

# # Сканируем музыку
# docker compose -f docker-compose.prod.yml exec api uv run flask --app radio_andrews.app:create_app scan-music
